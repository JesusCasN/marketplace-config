server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  datasource:
#    url: jdbc:postgresql://localhost:5435/productosdb
#    username: postgres
#    password: Chuchoska387
#    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate  # el esquema lo gestiona Flyway (db/migration); Hibernate solo comprueba que coincide
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
#  secret: MiSuperClaveSecretaParaJWT12345678901234567890
